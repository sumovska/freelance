# This module would probably either call into gettext, or use some other localisation framework.
# Alternatively, consider subclassing controls so that they call the localisation framework.

def T(english):
    return english
